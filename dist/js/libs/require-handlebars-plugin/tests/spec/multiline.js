define(["hbs!"+require.toUrl("tests/templates/multiline")],function(t){describe("Template with multiline comment and quotes",function(){it("can load a template containing metadata with linebreaks and quotes",function(){expect(t).to.be.a("function")}),it("can parse metadata containing linebreaks",function(){var e=t.meta;expect(e).to.be.an("object");var n=e.description;expect(n).to.be.a("string"),expect(n).to.contain("multiline comment")}),it("can parse metadata containing quotes",function(){var e=t.meta;expect(e).to.be.an("object");var n=e.description;expect(n).to.be.a("string"),expect(n).to.contain('"quotes"')})})});